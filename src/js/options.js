
import {icons, txt} from './constants';

const {enterIcon, clearIcon, closeIcon} = icons;
const {placeholderText, labelText} = txt;

const defaultOptions = {
    action: '',
    method: 'get',
    closeIcon,
    clearIcon,
    enterIcon, 
    placeholderText,
    labelText 
};

export default defaultOptions;